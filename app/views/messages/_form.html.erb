<%#= form_with model: [@channel, Message.new], data: { action: "ajax:success->channel#clearMessage" } do |form| %>
  <%# if message.errors.any? %>
    <!-- <div id="error_explanation">
      <ul>
        <% message.errors.full_messages each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div> -->
  <%# end %>

  <!-- <div class="message-editor flex rounded-lg border-2 border-grey overflow-hidden">
    <span class="text-3xl text-grey border-r-2 border-grey p-2">
      <i class="fas fa-plus"></i>
    </span> -->
    <%#= form.hidden_field :channel_id, value: channel.id %>
    <%#= form.hidden_field :space_id, value: channel.space_id %>
    <%# <input type="text" class="w-full px-4" placeholder="Message #general" /> %>
    <%#= form.text_field :content, :class => "w-full px-4" , placeholder: "Type your message", data: { target: "channel.newMessage" }%>
  <!-- </div> -->
    <%#= form.submit "Sent Message", class: "send-btn" %>

<%# end %>

