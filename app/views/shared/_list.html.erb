<div class="home_area_signed">
    <div class="list_container">
        <div class="list_caption">就緒！</div>
        <table class="list_table">
            <td id="list_head" colspan="4" style="border-spacing:10px;" border="1">
                <%= current_user.email %> 的工作空間
            </td>
            <% channels.each do |channel| %>
                <tr>
                    <td id="list_spaces">
                            <%= channel.space.name %>
                        </td>
                        <td id="list_spaces_start_stork">
                            <%= link_to "啟動STORK" , space_channel_path(channel.space.id ,channel.id) %>
                    </td>
                    <tr>
                        <td></td>
                    </tr>
                    </tr>
                <!-- <td style="display: none;">
                                    <span style="font-size:12px;"><%#= link_to "編輯", edit_page_path(s.id) %>／<%#= link_to "刪除", page_path(s.id),
                                method: 'delete',
                                "data-confirm": "確定嗎？" %></span>
                                </td> -->
                <% end %>
        </table>
        <table class="list_table">
            <tr id="list_head">
                <td id="list_spaces_start_stork">
                    <%= link_to "建立新空間" , stork_step1_path %>
                </td>
                <td>
                    <%= link_to "登出" , destroy_user_session_path, method: :delete %>
                </td>
            </tr>
        </table>
        <div>
            　
        </div>

    </div>
</div>