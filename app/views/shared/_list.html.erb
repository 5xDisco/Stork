<div class="home-area-signed">
    <div class="list-container">
        <div class="list-caption">就緒！</div>
        <table class="list-table">
            <td id="list-head" colspan="4" style="border-spacing:10px;" border="1">
                <%= current_user.email %> 的工作空間
            </td>
            <% spaces.each do |s| %>
                <tr>                   
                    <td id="list-spaces">icons　<%= s.name %>
                    </td>
                    <td id="list-spaces-start-stork">
                        <%= link_to "啟動STORK"%>
                    </td>
                    <tr>
                        <td></td>
                    </tr>
                </tr>
                <!-- <tr>
                    <td style="display: none;">
                            <%= link_to "編輯", edit_page_path(s.id) %>／<%= link_to "刪除", page_path(s.id),
                                method: 'delete',
                                "data-confirm": "確定嗎？" %>
                    </td>
                </tr> -->
                <% end %>
        </table>
        <table class="list-table">
            <tr id="list-head">
                <td id="list-spaces-start-stork">
                    <%= link_to "建立新空間" , stork_step1_path %>
                </td>
                <td>
                    <%= link_to "登出" , destroy_user_session_path, method: :delete %>
                </td>
            </tr>
        </table>
        <div>
            　
        </div>

    </div>
</div>